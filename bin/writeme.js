#!/usr/bin/env node
"use strict";

// WRITEME is still in development. This entrypoint reserves the `writeme`
// command and reports honest status until the compiler ships in 0.1.0.
console.log(`WRITEME ${require("../package.json").version} — not released yet.

WRITEME compiles WRITEME.md into README.md, so shared documentation
can be written once and included across every package in a monorepo.

The compiler is still being built. Nothing is functional in this version.

  Progress: https://github.com/dltldn333/WRITEME
`);
